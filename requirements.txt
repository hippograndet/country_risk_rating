pandas>=1.3
numpy>=1.20
scikit-learn>=1.2
xgboost>=1.6
mlflow>=2.0
joblib>=1.1
wbgapi>=1.0

# PDF extraction (camelot requires the 'cv' backend; also needs system-level
# ghostscript: `brew install ghostscript` on macOS, `apt install ghostscript` on Linux)
camelot-py[cv]>=0.10

# Interpretability — pinned loosely to avoid conflicts with xgboost/numpy ABI.
# If install fails, try: pip install shap==0.44.* numpy==1.26.*
shap>=0.41

# Notebooks
notebook>=7.0

# Dev tools
pytest>=7.0
flake8>=6.0
